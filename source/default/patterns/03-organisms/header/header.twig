{# SETTINGS #}
{% set header_classes = ([
  'header-offset bg-teal-200 w-full z-30 top-0 text-white',
  variant == 'fixed'? 'fixed h-12 w-full',
  variant == 'sticky'? 'sticky',
]) | sort | join(' ') | trim %}

{# TEMPLATE #}
<header class="{{ header_classes }}">
  <div class="w-60 h-full flex items-center max-w-xs md:max-w-7xl">
    {% include "@atoms/logo/logo.twig" only %}
    <span class="text-xs text-gray-300">v1.0</span>
  </div>
</header>
